package com.tryfinally.sort.Points;


public class Main {
    /*public static void main(String[] args) {
        comparatorTest();

    }

    private static void comparatorTestAnonyClass() {
        var p1 = new Point(13, 7);
        var p2 = new Point(5, 31);

        var c = new Comparator(){

            @Override
            public int compare(Object a, Object b) {
                return 0;
            }

            @Override
            public int compare(Point a, Point b) {
                return a.y - b.y;
            }
        };

        var m = Utils.max(p1, p2, c);
        System.out.println(m);

        Utils.max(p1, p2, new Comparator() {
            @Override
            public int compare(Point a, Point b) {
                return a.y - b.y;
            }
        });
    }


    private static void comparatorTestInnerClass() {
        var p1 = new Point(13, 7);
        var p2 = new Point(5, 31);

        class CompareByAdd implements Comparator {

            @Override
            public int compare(Point a, Point b) {
                return (a.x+a.y) - (b.x+b.y);
            }
        }

        var m = Utils.max(p1, p2, new CompareByAdd());
        System.out.println(m);
    }


    private static void comparatorTest() {
        var p1 = new Point(13, 7);
        var p2 = new Point(5, 31);

        var m = Utils.max(p1, p2, new PointComparatorByX());
        System.out.println(m);
    }*/
}
